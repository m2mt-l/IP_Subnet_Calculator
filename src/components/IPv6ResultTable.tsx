import React, { FC } from "react";

const IPv6ResultTable: FC<{ ipv6Address: string; subnet: string }> = ({ ipv6Address, subnet }) => {
    return <div>IPv6ResultTable</div>;
};

export default IPv6ResultTable;
